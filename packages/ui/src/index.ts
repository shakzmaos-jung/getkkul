// @getkkul/ui — scaffold (empty).
// Intended (NEW in M1): Linear design-token system (Tailwind v4 @theme + [data-theme]),
// theme registry (linear only at launch), ThemeProvider, and shared admin components.
// These are authored fresh for admin — NOT extracted from web's Geist theme.
export {};
