// @getkkul/config — scaffold (empty).
// Intended (JIT, when admin needs the same presets): shared eslint / tsconfig / tailwind
// presets so apps/web + apps/admin dedupe tooling. Deferred in M0 to avoid a shared
// tsconfig base shadowing apps/web's @/* alias resolution (regression guard).
export {};
