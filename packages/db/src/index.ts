// @getkkul/db — scaffold (empty).
// Intended (JIT, M2+): shared Supabase client factories + generated database.types.
// Nothing is extracted here in M0 (strategy A: skeleton-first, zero-regression).
// The server-only / non-server-only client split (createPipelineClient) must be
// preserved when content is moved here.
export {};
